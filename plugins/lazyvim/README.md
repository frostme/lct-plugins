# lazyvim Plugin for LCT

This plugin bootstraps a Neovim setup from the LazyVim starter repository.

Before installing, it backs up any existing Neovim config, local state, and cache directories so a previous setup is not overwritten in place. It is useful for quickly standardizing on a modern Neovim distribution inside an LCT-managed environment.
